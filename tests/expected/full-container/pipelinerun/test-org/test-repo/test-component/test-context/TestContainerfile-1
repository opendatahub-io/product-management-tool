# PLACEHOLDER — generated by PMT (aipcc-product-management) during Konflux onboarding.
# This component has no real build content yet. Replace this file with the real
# Containerfile when the component build is ready, and ping the AIPCC Productization
# team so the build can be enabled (drop `disabled: true` from the PMT config).
FROM scratch

# Placeholder arg, read from the build-args file referenced by this pipelinerun.
ARG PLACEHOLDER
